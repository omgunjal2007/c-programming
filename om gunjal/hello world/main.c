#include <stdio.h>
#include <stdlib.h>

int main() {
int num1,num2,sum;


    printf("enter the number:");

    scanf("%d%d",&num1 &num2);

    if ( num>0)
        printf("number is postive\n:");
        else
            printf("number is negative\n:");
    return 0;
}
